// The API is served by this same Next.js app under /api/v1, so requests are
// always same-origin — no CORS, and the httpOnly refresh cookie is sent
// automatically.
export const API_BASE_URL = "/api/v1";
